{
  "name": "permit-lol",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "react-scripts": "0.4.0"
  },
  "dependencies": {
    "react": "^15.3.1",
    "react-dom": "^15.3.1",
    "react-router": "^2.8.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "npm run build && git push origin :gh-pages && git subtree push --prefix build origin gh-pages"
  },
  "eslintConfig": {
    "extends": "./node_modules/react-scripts/config/eslint.js"
  }
}
